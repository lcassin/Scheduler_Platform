<Window x:Class="MermaidEditor.FindReplaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find and Replace" 
        Height="220" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ThemeBackgroundBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <Window.Resources>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ThemeToolbarBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ThemeHoverBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource ThemePurpleAccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="DialogTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ThemeToolbarBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ThemeForegroundBrush}"/>
        </Style>
        
        <Style x:Key="DialogCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Find row -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Find:" 
                   Foreground="{DynamicResource ThemeForegroundBrush}" 
                   VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="FindTextBox" Grid.Row="0" Grid.Column="1" 
                 Style="{StaticResource DialogTextBoxStyle}" 
                 Margin="0,0,8,8"
                 TextChanged="FindTextBox_TextChanged"/>
        <Button Grid.Row="0" Grid.Column="2" Content="Find Next" 
                Style="{StaticResource DialogButtonStyle}" 
                Click="FindNext_Click" Margin="0,0,0,8"/>
        
        <!-- Replace row -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Replace:" 
                   Foreground="{DynamicResource ThemeForegroundBrush}" 
                   VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="ReplaceTextBox" Grid.Row="1" Grid.Column="1" 
                 Style="{StaticResource DialogTextBoxStyle}" 
                 Margin="0,0,8,8"/>
        <Button Grid.Row="1" Grid.Column="2" Content="Replace" 
                Style="{StaticResource DialogButtonStyle}" 
                Click="Replace_Click" Margin="0,0,0,8"/>
        
        <!-- Options row -->
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,8,8">
            <CheckBox x:Name="MatchCaseCheckBox" Content="Match case" 
                      Style="{StaticResource DialogCheckBoxStyle}" Margin="0,0,16,0"/>
            <CheckBox x:Name="WholeWordCheckBox" Content="Whole word" 
                      Style="{StaticResource DialogCheckBoxStyle}"/>
        </StackPanel>
        <Button Grid.Row="2" Grid.Column="2" Content="Replace All" 
                Style="{StaticResource DialogButtonStyle}" 
                Click="ReplaceAll_Click" Margin="0,0,0,8"/>
        
        <!-- Status row -->
        <TextBlock x:Name="StatusText" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                   Foreground="{DynamicResource ThemeDisabledForegroundBrush}" 
                   Margin="0,8,0,0"/>
        <Button Grid.Row="3" Grid.Column="2" Content="Close" 
                Style="{StaticResource DialogButtonStyle}" 
                Click="Close_Click" Margin="0,8,0,0"/>
    </Grid>
</Window>
