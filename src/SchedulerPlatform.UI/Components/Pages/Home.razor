@rendermode InteractiveServer
@inject NavigationManager NavManager

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            NavManager.NavigateTo("/schedules", replace: true);
        }
    }
}
